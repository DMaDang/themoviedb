<div class="container mt-5 pt-4 pb-4">
    <h3 class="h3-title">Chi tiết tài khoản</h3>
    <div class="card">
        <div class="card-header">Xin chào, {{account.username}}!</div>
        <div class="card-body">
            <p><strong>Tên người dùng:</strong> {{#if account.name}} {{account.name}} {{else}} N/A {{/if}}</p>
            <p><strong>ID:</strong> {{account.id}}</p>
            <p><strong>Quốc gia</strong> {{account.iso_3166_1}}</p>
            <p><strong>Include Adult Content:</strong> {{#if account.include_adult}}Yes{{else}}No{{/if}}</p>
        </div>
    </div>
    <a href="/" class="btn btn-primary mt-3">Trở về trang chủ</a>
</div>
